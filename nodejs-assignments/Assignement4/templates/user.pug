extends layout/main-layout.pug

block content
    main 
        h2(style="margin-bottom: 0") List of Names Entered
        ul(style="margin:0") 
           each name in names 
            li.user-names #{name}